name: Publish

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  publish:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v1
      - run: |
          brew install docker
          colima start
          docker ps
          echo $DOCKER_HOST
